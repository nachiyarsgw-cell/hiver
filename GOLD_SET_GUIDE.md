# Gold Set Guide

The file `data/gold_set.csv` contains 198 examples, within the assignment requirement of 150–250.

`gold_intent` values are **seed labels generated by deterministic keyword rules** and are marked `SEED_REVIEW_REQUIRED`. For an honest submission, a human annotator must review/correct these labels before describing the set as hand-labelled.
